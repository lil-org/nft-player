{
  "address": "0x47a91457a3a1f700097199fd63c039c4784384ab",
  "name": "Ge0metric Origamix",
  "abId": "121",
  "chain": "ethereum",
  "kind": "p5js100",
  "value": "class Random {\r\n  constructor() {\r\n    this.useA = false;\r\n    let sfc32 = function (uint128Hex) {\r\n      let a = parseInt(uint128Hex.substring(0, 8), 16);\r\n      let b = parseInt(uint128Hex.substring(8, 16), 16);\r\n      let c = parseInt(uint128Hex.substring(16, 24), 16);\r\n      let d = parseInt(uint128Hex.substring(24, 32), 16);\r\n      return function () {\r\n        a |= 0;\r\n        b |= 0;\r\n        c |= 0;\r\n        d |= 0;\r\n        let t = (((a + b) | 0) + d) | 0;\r\n        d = (d + 1) | 0;\r\n        a = b ^ (b >>> 9);\r\n        b = (c + (c << 3)) | 0;\r\n        c = (c << 21) | (c >>> 11);\r\n        c = (c + t) | 0;\r\n        return (t >>> 0) / 4294967296;\r\n      };\r\n    };\r\n    // seed prngA with first half of tokenData.hash\r\n    this.prngA = new sfc32(tokenData.hash.substring(2, 34));\r\n    // seed prngB with second half of tokenData.hash\r\n    this.prngB = new sfc32(tokenData.hash.substring(34, 66));\r\n    for (let i = 0; i < 1e6; i += 2) {\r\n      this.prngA();\r\n      this.prngB();\r\n    }\r\n  }\r\n  // random number between 0 (inclusive) and 1 (exclusive)\r\n  random_dec() {\r\n    this.useA = !this.useA;\r\n    return this.useA ? this.prngA() : this.prngB();\r\n  }\r\n  // random number between a (inclusive) and b (exclusive)\r\n  random_num(a, b) {\r\n    return a + (b - a) * this.random_dec();\r\n  }\r\n  // random integer between a (inclusive) and b (inclusive)\r\n  // requires a < b for proper probability distribution\r\n  random_int(a, b) {\r\n    return Math.floor(this.random_num(a, b + 1));\r\n  }\r\n  // random boolean with p as percent liklihood of true\r\n  random_bool(p) {\r\n    return this.random_dec() < p;\r\n  }\r\n  // random value in an array of items\r\n  random_choice(list) {\r\n    return list[this.random_int(0, list.length - 1)];\r\n  }\r\n}\r\n\r\nlet offset = 80;\r\nlet grid;\r\nlet size, margin;\r\nlet R = new Random();\r\nlet seed;\r\n\r\nfunction setup() {\r\n  grid = floor(R.random_int(2, 10));\r\n  createCanvas(windowWidth, windowHeight);\r\n  angleMode(DEGREES);\r\n  size = (width - offset * 2) / grid;\r\n  margin = size / floor(R.random_int(5, 30));\r\n  seed = R.random_int(0, 99999);\r\n  randomSeed(seed);\r\n  noiseSeed(seed);\r\n  noLoop();\r\n}\r\n\r\nfunction draw() {\r\n  background(255);\r\n  for (let j = 0; j < grid; j++) {\r\n    for (let i = 0; i < grid; i++) {\r\n      push();\r\n      translate(offset + i * size + size / 2, offset + j * size + size / 2);\r\n      if (R.random_bool(0.5)) {\r\n        rotate(0);\r\n      } else {\r\n        rotate(90);\r\n      }\r\n      dTri(0, 0, size - margin, 4);\r\n      rotate(180);\r\n      dTri(0, 0, size - margin, 4);\r\n      pop();\r\n    }\r\n  }\r\n}\r\n\r\nfunction dTri(x, y, s, n) {\r\n  let hs = s / 2;\r\n  push();\r\n  translate(x, y);\r\n  stroke(0);\r\n  if (R.random_bool(0.5)) {\r\n    fill(0);\r\n  } else {\r\n    fill(255);\r\n  }\r\n\r\n  triangle(-hs, -hs, hs, -hs, -hs, hs);\r\n  pop();\r\n  n--;\r\n\r\n  if (n >= 0) {\r\n    if (R.random_bool(0.5)) {\r\n      push();\r\n      translate(x, y - hs);\r\n      rotate(-135);\r\n      dTri(0, 0, s * sqrt(0.5), n);\r\n      pop();\r\n    }\r\n\r\n    if (R.random_bool(0.5)) {\r\n      push();\r\n      translate(x - hs, y);\r\n      rotate(135);\r\n      dTri(0, 0, s * sqrt(0.5), n);\r\n      pop();\r\n    }\r\n  }\r\n}\r\n",
  "collectionIdOverride": "0x47a91457a3a1f700097199fd63c039c4784384ab-dev-good-42161-121",
  "isDevelopmentPreview": true
}
